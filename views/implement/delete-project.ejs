<!-- Modal 1 My profile-->
<div class="modal fade" id="deleteProjectModal" tabindex="-1" role="dialog" aria-labelledby="deleteProjectModal"
    aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Delete Project</h5>
                <button type="button" class="btn-close" data-dismiss="modal" aria-label="Close">
                </button>
            </div>
            <div class="modal-body">
                <form id="addmember" action="/home/deleteproject" method="post">
                    <h3 class="text-danger">Are you sure?</h3>
                    <div class="form-group">
                        <label for="linkname" class="col-form-label">Name:</label>
                        <input type="text" class="form-control" id="linkname" disabled="" name="linkname"value="<%= typeof project.projectname != 'undefined' ? project.projectname : '' %>">
                    </div>
                    <input type="hidden" class="form-control" id="id" name="id" value="<%= typeof project._id != 'undefined' ? project._id : '' %>">
                    
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                <button type="submit" class="btn btn-primary">Submit</button>
            </div>
                </form>
            </div>
        </div>
    </div>
</div>